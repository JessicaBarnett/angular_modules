import { Routes } from '@angular/router';
import { FeatureComponent } from './feature/feature-component.component';

export const appRoutes: Routes = [{
    path: 'feature',
    component: FeatureComponent,
}];
